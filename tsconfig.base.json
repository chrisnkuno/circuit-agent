{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Compiler options every workspace in this repo shares. Each app and package extends it and adds only what is genuinely its own — a `lib` that includes the DOM, a JSX runtime, a path map. Anything set here is set once, so 'strict' cannot quietly mean two different things in two directories.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["es2022"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true
  }
}
