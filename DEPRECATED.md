# This repository is deprecated

**Circuit-Nova / `circuit-agent` is no longer developed.** Work continues in
[**chrisnkuno/archymedes-cli**](https://github.com/chrisnkuno/archymedes-cli).

Final commit here: `0c0b575` (2026-08-29). Development moved to `archymedes-cli`
on 2026-08-31; this tree is kept read-only for history only.

## Where things went

| Deprecated here | Successor |
| --- | --- |
| `packages/nova-cli` (`@circuit-nova/nova-cli`) | `packages/archymedes-cli` |
| `packages/nova-state` | `packages/archymedes-state` |
| `packages/agent-core` (`@circuit-nova/nova-core`) | `packages/core` |
| control plane / billing / routing | the `archymedes-*` satellite repos |

Both npm packages are deprecated on the registry. Do not open issues or pull
requests against this repository.
