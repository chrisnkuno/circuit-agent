{"default":{"identifier":"default","description":"Scoped to the main window. No shell permissions: the sidecar is spawned by Rust via std::process, so the webview needs no ability to execute anything. No store permissions either — the settings store is only ever touched from Rust, and the ACL governs webview calls.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open","updater:allow-check","updater:allow-download-and-install"]}}