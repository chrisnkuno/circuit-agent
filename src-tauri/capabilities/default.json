{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Scoped to the main window. Deliberately no shell permissions: the sidecar is spawned by Rust via std::process, so the webview needs no ability to execute anything.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:default",
    "updater:default"
  ]
}
